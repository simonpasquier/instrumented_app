services:
- docker

language: go

go:
- "1.x"

env:
  GO111MODULE: "on"

script:
- go get github.com/prometheus/promu@v0.2.0
- make docker

deploy:
  provider: script
  script: bash scripts/docker-push.sh
  on:
    branch: master
